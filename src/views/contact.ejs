<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= title %> </title>

    <!-- Agregamos porcion de codigo que contiene head.ejs el cual es un archivo link:Css -->
    <%- include("partials/head") %>
</head>
<body>

    <!-- Importamos mediante ejs, la porcion de codigo que se encuentra en navigation.ejs -->
    <%- include("partials/navigation") %>

    <div class="container vh-100 d-flex justify-content-center align-items-center">
        <div class="card bg-dark textw-white text-center">
            <div class="card-header">
                <h3>Contact me</h3>
            </div>
            <div class="card-body text-left">
                <form>

                    <div class="mb-3">
                        <label for="email">Email</label>
                    <input type="email" placeholder="Email" class="form-control">
                    </div>

                    <div class="mb-3">
                        <label for="mesage">Message:</label>
                    <textarea name="Message" rows="5" class="form-control" placeholder="Write a Message"></textarea>
                    </div>

                    <button class="btn btn-primary btn-block">Save</button>

                </form>
            </div>
        </div>
    </div>

    <!-- Footer de la pagina -->
    <%- include("partials/footer") %>

</body>
</html>